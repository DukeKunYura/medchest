import React from 'react';
import Navigation from './Navigation';

/**
 * Родительский компонент, возвращает компонент с навигацией
 */
export default function Main() {
    return (
        <Navigation />
    );
}